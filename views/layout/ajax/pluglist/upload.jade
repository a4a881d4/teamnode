html
  body
    // ajax style
    form(action='?c=pluglist&a=uploaded', method='post', enctype='multipart/form-data')
      div
        button.btn.btn-primary.pull-right(type='submit') 上传
        input#pfile(type='file', name='pfile')
      .line-large
      div 请只安装可信任来源的插件，ZIP包根目录下直接放app.php，无需再用子目录。
